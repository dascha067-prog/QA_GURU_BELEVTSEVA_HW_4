# QA_GURU_BELEVTSEVA_HW_4

# 🐍 Основы Python — шпаргалка

## 🔹 Переменные и константы

```python
x = 10            # переменная
PI = 3.14         # константа (по соглашению — заглавными буквами)
```

* Имя переменной: латиница, цифры, подчёркивание (`_`)
* Нельзя начинать с цифры
* Python — **чувствителен к регистру** (`age` ≠ `Age`)

---

## 🔹 Ввод и вывод данных

```python
name = input("Введите имя: ")
print("Привет,", name)
```

* `input()` — всегда возвращает строку
* `print()` — выводит данные на экран

---

## 🔹 Операторы

### Сравнения:

`==`, `!=`, `>`, `<`, `>=`, `<=`

```python
x = 5
print(x > 3)  # True
```

### Логические:

`and`, `or`, `not`

```python
a, b = True, False
print(a and not b)  # True
```

### Присваивания:

`=`, `+=`, `-=`, `*=`, `/=`

```python
x = 10
x += 5   # x = 15
```

---

## 🔹 Типизация

```python
a = 5          # int
b = 3.5        # float
c = "Hello"    # str
d = True       # bool
```

* Узнать тип: `type(a)`
* Преобразование типов:

  ```python
  int("5") → 5
  float("3.14") → 3.14
  str(10) → "10"
  ```

---

## 🔹 Типы данных

### 🔸 Числа (numbers)

```python
import math, random
math.sqrt(9)       # 3.0
random.randint(1,5)
round(3.14159, 2)  # 3.14
```

### 🔸 Строки (strings)

```python
s = "Python"
print(s[0])       # P
print(s[-1])      # n
print(s[::2])     # Pto
print(s.upper())  # PYTHON
```

---

### 🔸 Списки (lists)

```python
nums = [1, 2, 3]
nums.append(4)
nums.remove(2)
print(nums)  # [1, 3, 4]
```

---

### 🔸 Множества (set)

```python
s = {1, 2, 3}
s.add(4)
print(s)  # {1, 2, 3, 4}
```

---

### 🔸 Словари (dict)

```python
user = {"name": "Daria", "age": 25}
print(user["name"])
user["age"] = 26
```

---

### 🔸 Кортежи (tuple)

```python
coords = (10, 20)
print(coords[0])  # 10
```

---

### 🔸 Frozenset
Frozenset это неизменяемое множество, которое задается следующим образом:
```python
f = frozenset([1, 2, 3]) 
f = frozenset([1, 2, 3, 4, 5])
```
Frozenset поддерживает те же операции, что и множества, но при этом не поддерживает операции, которые изменяют множество

---

### 🔸 Изменяемость

| Тип             | Изменяемый |
|-----------------| ---------- |
| **list** (списки) | ✅          |
| **set** (множество) | ✅          |
| **dict** (**словари)  | ✅          |
| **tuple** (кортежи) | ❌          |
| **frozenset**       | ❌          |
| **str** (строки)    | ❌          |

